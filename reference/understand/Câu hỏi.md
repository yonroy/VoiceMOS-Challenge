1. Gemini hiện tại khi chạy qua tập dataset này để chấm điểm theo baseline btc thì đang bị tốn token. Có thể dùng phương án thay thế khác không? 


2. Có phải mỗi số liệu (QMOS, EMOS, CAT, VAD) đều là một model hay chỉ cần machine learning?