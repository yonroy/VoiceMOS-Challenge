# Thư viện cho PYTHON BACKEND của Triton (cài vào image server).
# LƯU Ý: image tritonserver:24.08-py3 CHỈ có PyTorch *backend* (libtorch C++),
# KHÔNG có sẵn gói Python `torch` cho PYTHON backend → model.py `import torch` sẽ
# lỗi "No module named 'torch'". Vì vậy PHẢI cài torch/torchaudio ở đây.
# Pin 2.4.* cho khớp libtorch của Triton 24.08 (CUDA 12.x).
torch==2.4.1
torchaudio==2.4.1
# Chặn TRẦN < 4.50: từ 4.50 transformers vá CVE-2025-32434 → CẤM torch.load khi torch < 2.6.
# microsoft/wavlm-large chỉ có pytorch_model.bin (phải torch.load) → bản mới sẽ chặn nạp model.
# Giữ torch 2.4.1 (khớp libtorch Triton 24.08) nên phải dùng transformers cũ hơn.
transformers>=4.40,<4.50
huggingface_hub>=0.23
safetensors>=0.4
librosa>=0.10
soundfile>=0.12
numpy>=1.24,<2.0
scipy>=1.10
