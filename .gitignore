# Secrets / môi trường
.env
*.key

# Môi trường ảo Python
.venv/
venv/
env/

# Repo baseline clone riêng (nặng, là git repo độc lập — không commit)
baselines/

# Python cache
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Dữ liệu thô (nặng — upload lên Kaggle, không commit)
data/
*.tar.gz

# Checkpoint & feature cache (RẤT NẶNG ~4.7GB — lưu trên Kaggle Dataset / HF, KHÔNG commit)
cache/
*.pt
*.npz

# Audio input/demo (nặng — không commit)
100audio/
*.wav

# Video demo (nặng — không commit; để trên HF Space/Drive)
*.mp4

# Output / submission
*.zip
*.out
predictions*.csv
answer.txt
outputs/

# Hệ điều hành / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
